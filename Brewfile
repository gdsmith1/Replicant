# This file contains the bare minimum required to get Replicant up and running in the cloud.
# To run, use `brew bundle` from the root of the project.
brew "awscli"       # AWS Command Line Interface
brew "terraform"    # Infrastructure as code tool
brew "terragrunt"   # Thin wrapper for Terraform that provides extra tools
brew "make"         # Utility for directing compilation
